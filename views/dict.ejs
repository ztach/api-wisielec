<link rel="stylesheet" href="/css/styles_resp_flex.css">
<% include ./partials/head %>

</head> 

<body>

<header class="app-header">
  <% include ./partials/header %>
</header>

<section class="hero">
        <div class="background-image" ></div>
<div class="Text">
  <h3>Tabela: Dict</h3>
  <p>Tabela 'Dict' zwiera pola: </p>
      <ul>
          <li> <em>hasło</em>  - jest hasłem które ma odgadnąć grający </li>
          <li> <em>typ hasła</em>  - jest podpowiedzią dla grającego. Ma przybliżyć rodzaj hasł </li>
          <li> dwa następne pola są to ForiKey do tabel <em>Typ</em>  i <em>Polecieni</em>  </li>
      </ul>
</div>  
    
  
    <table class="redTable">
        <thead>
            <tr>
                <th>id</th>
                <th>hasło</th>
                <th>typ hasła</th>
                <th>typ id</th>
                <th>polecenie id</th>
                <th> </th>
            </tr>
        </thead>
        <tbody>
          <% for(var i=0; i<dataType.length; i++) {%>
              <tr>
                  <td id="<%= dataType[i].id %>" >
                      <%= dataType[i].id %>
                  </td> 
                  <td> 
                          <a href="/dict/<%= dataType[i].id %>">
                      <%= dataType[i].sl %> 
                          </a>
                  </td>

                  <td> 
                        <a href="/dict/<%= dataType[i].id %>">
                    <%= dataType[i].gt %> 
                        </a>
                </td>

                <td> 
                        <a href="/dict/<%= dataType[i].id %>">
                    <%= dataType[i].typ_id %> 
                        </a>
                </td>

                <td> 
                        <a href="/dict/<%= dataType[i].id %>">
                    <%= dataType[i].polecenie_id %> 
                        </a>
                </td>


                  <td>
                      <% if(!isAll && isSingle) {  %>
                              <a href="/dict">
                                  <span>&#8592;</span>
                              </a>
                      <% } else { %>
                          <a href="/">
                              <span>&#8592;</span>
                          </a>
                      <% }  %>
                  </td>
              </tr>
          <%}%>
          </tbody>
      </table>
    
</section>

<script type="text/javascript" src="js/tools.js"></script>
